/// <reference types="jest" />
/// <reference types="@testing-library/jest-dom" />

// Makes the jest-dom matchers (toHaveFocus, toBeInTheDocument, …) and the Jest
// globals visible to TypeScript across the test suite. The matchers are wired
// in at runtime by jest.setup.js.
