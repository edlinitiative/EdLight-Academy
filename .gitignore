# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/dist
/public/js
/public/css
/public/*.html
/public/*.png
# Per-exam files are generated from public/exam_catalog.json by the
# `prebuild` hook (scripts/split_exam_catalog.mjs); no need to commit ~21MB.
/public/exams/

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local script checkpoints
.enrich_hints_checkpoint.json
.regen_bank_checkpoint.json
.regen_quizzes_checkpoint.json

# IDE
.idea/
.vscode/

# Claude Code
.claude/

# Private data (quizzes with answers)
/private_data/
*.swp

# Pipeline run artifacts (audit reports, fix logs, snapshots)
/artifacts/
*.swo.env
.venv/
.firebase/
.vercel
.env*
!.env.example
